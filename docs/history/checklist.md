# History Checklist

Быстрый справочник: паттерн → запись в `docs/history/`.
Открывать только при ошибке или неожиданном поведении (§1a CLAUDE.md).

| Паттерн проблемы | Запись |
|---|---|
| Новая Eloquent-модель без миграции | — |
| N+1 в Repository (забыли `with()`) | — |
| Тест не чистит БД (`RefreshDatabase` отсутствует) | — |
| PHPStan не видит тип возврата Eloquent-коллекции | — |
| Elasticsearch-индекс не найден в тесте | — |
> При добавлении записи HIST-XXX через `/history` — добавь строку в эту таблицу.
